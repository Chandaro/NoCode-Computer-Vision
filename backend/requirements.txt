# ─── Core backend ─────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn==0.32.1
sqlmodel==0.0.22
python-multipart==0.0.20
pillow>=10.0.0
aiofiles==24.1.0

# ─── Computer Vision ──────────────────────────────────────────────────────────
ultralytics==8.4.41
# torchvision is pulled in automatically by ultralytics (do NOT add it here — the
# installer installs the correct CUDA build separately and pins it via constraints)

# ─── PyTorch ──────────────────────────────────────────────────────────────────
# NOTE: PyTorch is NOT listed here because the correct variant depends on
# whether you have a CUDA-capable GPU.  The installer handles this for you.
#
# Manual install (choose ONE):
#   CPU only:          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA 11.8:         pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.0–12.4:    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   CUDA 12.5+ (RTX 40/50 series, Blackwell):
#                      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
